---
name: market-data
description: >-
  Answer market questions with Worklittle aggregates: counts, remote share, salary
  signals, top employers. Use for how many / trends / is this market hot — not for listing
  specific jobs to apply to (use job-search).
---

# Market data

## Critical
Prefer `get_market_overview`. Never page `search_jobs` just to count.

## Optional
A few example roles with small `limit` after the overview.

## Output
Market takeaway first, then numbers from the tool only.
